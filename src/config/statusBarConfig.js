import {StatusBar} from 'react-native';

// Para definir a cor de fundo da statusBar no android
StatusBar.setBackgroundColor('#DA552f');

// Para definir a staturBar como branca, ao invés do padrão preto, no iOS
StatusBar.setBarStyle('light-content');
